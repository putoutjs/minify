const l = [];
l.forEach((o) => {
    const c = o.sizer;
    
    g.forEach((v, w) => {
        if (v && v.length > 1) {
            const t = c.appendChild(document.createElement('span'));
        } else
            m[w] = y;
    });
}),
l.forEach((o) => {
    for (
        let c = o.sizer;
        y < g.length;
        y++
    )
        g[y] = c.children[m++];
});console.log('hello');
