const l=[];l.forEach(function (o) {
    var c = o.sizer;
    g.forEach(function (v, w) {
        if (v && v.length > 1) {
            var t = c.appendChild(document.createElement("span"));
        } else m[w] = y;
    });
}),
    l.forEach(function (o) {
        for (
            var c = o.sizer;
            y < g.length;
            y++
        )
            g[y] = c.children[m++];
    });console.log('hello')