{
    "match": {
        ".filesystem.json": {
            "eslint/convert-rc-to-flat": "on"
        }
    },
    "rules": {
        "putout/remove-useless-source-argument": "on"
    },
    "ignore": [
        "bundle",
        "benchmark/result",
        "benchmark/source"
    ]
}
